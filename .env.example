# Email Configuration
# Absender-Email (z.B. Gmail)
SMTP_EMAIL=deine-email@gmail.com

# Gmail App-Passwort (https://myaccount.google.com/apppasswords)
SMTP_PASSWORD=dein-app-passwort

# Empfänger (mehrere durch Komma getrennt)
RECIPIENTS=empfaenger1@gmail.com,empfaenger2@gmail.com

# Filter Configuration
# Wörter im Titel filtern (komma-getrennt, case-insensitive)
# Beispiel: schwein,rind,lamm
FILTER_WORDS=

# Allergene filtern (komma-getrennte Namen, nicht Nummern!)
# Das Script sucht automatisch die Nummern aus der PDF-Allergen-Liste
# Beispiele: soja,milch,erdnüsse,sellerie,senf,eier,fisch
# Leer lassen für keine Allergen-Filterung
FILTER_ALLERGENS=

# Debug-Modus (zeigt alle gefundenen Allergene aus PDF)
# true = Debug-Ausgabe aktivieren, false = normal
DEBUG=false
